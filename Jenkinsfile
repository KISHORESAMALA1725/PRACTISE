pipeline {
    agent any
    stages {
        stage ('This is First stage') {
            steps {
                script{
                    def course = 'K8S'
                    if (course == "${course}")
                    println ("welcome to ${course} - Course")
                    else
                    println ("Please enrool to ${course} - Course")
                }
            }
        }
    }
}
